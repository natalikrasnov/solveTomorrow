{
  "entities": {
    "IdeaSession": {
      "title": "IdeaSession",
      "description": "Represents a single custom crafted product design conceptualization session.",
      "type": "object",
      "properties": {
        "_id": {
          "type": "string",
          "description": "Unique id of the session."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who owns the session or 'anonymous_user'."
        },
        "userInput": {
          "type": "object",
          "description": "Raw inputs entered by the user."
        },
        "generatedIdea": {
          "type": "object",
          "description": "Visionary strategic roadmap designed by Agent 1."
        },
        "status": {
          "type": "string",
          "enum": ["IDEA_GENERATED", "CODE_GENERATING", "COMPLETED", "REJECTED"],
          "description": "Active generation pipeline state."
        },
        "generatedHtml": {
          "type": "string",
          "description": "Pristine self-contained HTML source code generated by Agent 2."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Server-side ISO timestamp."
        }
      },
      "required": ["_id", "userInput", "generatedIdea", "status", "createdAt"]
    }
  },
  "firestore": {
    "/sessions/{sessionId}": {
      "schema": "IdeaSession",
      "description": "Collection storing concept blueprints and responsive live sandbox drafts."
    }
  }
}
